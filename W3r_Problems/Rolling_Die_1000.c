#include"stdio.h"
#include"stdlib.h"
#include"time.h"

int roll(int *p_num)
{
    srand(time(NULL)); //seed the generating of different numbers 
    int r = rand() % *p_num + 1;
    for (int i = 0; i < 1000; i++)
    {

    }
    
}

int main()
{



}